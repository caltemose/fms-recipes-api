include editable-input-text.pug

mixin editableUrlInput(url, endpoint)
    .EditableUrlInput(data-endpoint=endpoint data-url=url)
        a(href=url).Source-Url [visit]
        +editableInputText(url, endpoint)
